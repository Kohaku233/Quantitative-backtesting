# Quantitative Backtesting

Local crypto quantitative backtesting platform for Binance `BTCUSDT` perpetual futures.

## Task 1 Baseline

- FastAPI backend smoke endpoint at `/api/health`
- React + Vite frontend smoke shell
- Python environment via `.venv`
- Frontend workspace under `frontend/`
